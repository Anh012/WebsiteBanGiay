@{
	string ImagePath = "~/wwwroot/images/" + item.HinhAnh;
}
<img src="@Url.Content(ImagePath)" width="100" class="img-thumnail" />
<br/>
@Html.DisplayFor(modelItem => item.HinhAnh)

//@Html.ActionLink("Chitiet", "Details", new {id=item.MaHang.Trim()}
//var hangs = db.Hangs.Select(p=>p);


//
public ActionResult Index (string searchString)
{
if (!String.IsNullOrEmpty(searchString)){
	hangs = hangs.Where(p=>p.TenHang.Contains(searchString));
}
}

@using (Html.BeginForm())
{
	<p>
		Tim mat hang: @Html.TextBox("SearchString")
		<input type="submit" value = "tim"/>
</p>

//

@using (Html.BeginForm("SearchBySalary","Nhanviens",FormMethod.Get))
{
	<div>
		Nhap luong: <input type="text" name="lowerBound" /> Den <input type="text" name="upperBound" />
		<input type="submit" />
</div>

public ActionResult SearchBySalary(double lowerBound, double upperBound)
{
	var nv = db.Nhanviens.Where(s=>s.Luong >= lowerBound && s.Luong <= upperBound);
	return View("Index",nv);
}